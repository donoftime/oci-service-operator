---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: ocivaults.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: OciVault
    listKind: OciVaultList
    plural: ocivaults
    singular: ocivault
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the OciVault
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the OciVault
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: OciVault is the Schema for the ocivaults API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OciVaultSpec defines the desired state of OciVault
            properties:
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the Vault
                maxLength: 255
                minLength: 1
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              displayName:
                description: DisplayName is a user-friendly name for the Vault
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: VaultId is the OCID of an existing Vault to bind to (optional;
                  if omitted, a new vault is created)
                maxLength: 255
                minLength: 1
                type: string
              key:
                description: Key is an optional key to create and manage within the
                  vault
                properties:
                  displayName:
                    description: DisplayName is a user-friendly name for the key
                    type: string
                  id:
                    description: KeyId is the OCID of an existing key to bind to (optional;
                      if omitted, a new key is created)
                    maxLength: 255
                    minLength: 1
                    type: string
                  keyShape:
                    description: KeyShape defines the cryptographic properties of
                      the key
                    properties:
                      algorithm:
                        description: Algorithm is the encryption algorithm for the
                          key (AES, RSA, ECDSA)
                        enum:
                        - AES
                        - RSA
                        - ECDSA
                        type: string
                      length:
                        description: 'Length is the key length in bytes (AES: 16/24/32,
                          RSA: 256/384/512, ECDSA: 32/48/66)'
                        minimum: 16
                        type: integer
                    required:
                    - algorithm
                    - length
                    type: object
                required:
                - displayName
                - keyShape
                type: object
              vaultType:
                description: VaultType is the type of vault (DEFAULT or VIRTUAL_PRIVATE)
                enum:
                - DEFAULT
                - VIRTUAL_PRIVATE
                type: string
            required:
            - compartmentId
            - displayName
            - vaultType
            type: object
          status:
            description: OciVaultStatus defines the observed state of OciVault
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
