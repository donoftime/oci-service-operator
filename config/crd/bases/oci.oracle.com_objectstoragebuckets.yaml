---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: objectstoragebuckets.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: ObjectStorageBucket
    listKind: ObjectStorageBucketList
    plural: objectstoragebuckets
    singular: objectstoragebucket
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: BucketName
      priority: 1
      type: string
    - description: status of the ObjectStorageBucket
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: namespace/name identifier of the ObjectStorageBucket
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ObjectStorageBucket is the Schema for the objectstoragebuckets
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObjectStorageBucketSpec defines the desired state of ObjectStorageBucket
            properties:
              accessType:
                description: 'AccessType controls public access: NoPublicAccess, ObjectRead,
                  ObjectReadWithoutList, ObjectWrite'
                type: string
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the bucket
                maxLength: 255
                minLength: 1
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: BucketId is the composite identifier "namespace/bucketName"
                  of an existing bucket to bind to (optional)
                maxLength: 255
                minLength: 1
                type: string
              name:
                description: Name is the name of the bucket
                type: string
              namespace:
                description: Namespace is the OCI Object Storage namespace (auto-resolved
                  from tenancy if empty)
                type: string
              storageType:
                description: 'StorageType is the storage tier: Standard or Archive
                  (default: Standard)'
                type: string
              versioning:
                description: 'Versioning controls object versioning: Enabled or Suspended'
                type: string
            required:
            - compartmentId
            - name
            type: object
          status:
            description: ObjectStorageBucketStatus defines the observed state of ObjectStorageBucket
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
