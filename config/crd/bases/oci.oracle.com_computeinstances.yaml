---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: computeinstances.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: ComputeInstance
    listKind: ComputeInstanceList
    plural: computeinstances
    singular: computeinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ComputeInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ComputeInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ComputeInstance is the Schema for the computeinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComputeInstanceSpec defines the desired state of ComputeInstance
            properties:
              availabilityDomain:
                description: AvailabilityDomain is the availability domain where the
                  instance runs.
                type: string
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the instance.
                maxLength: 255
                minLength: 1
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              displayName:
                description: DisplayName is a user-friendly name for the instance.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: ComputeInstanceId is the OCID of an existing Compute
                  Instance to bind to (optional).
                maxLength: 255
                minLength: 1
                type: string
              imageId:
                description: ImageId is the OCID of the image used to boot the instance.
                maxLength: 255
                minLength: 1
                type: string
              shape:
                description: Shape is the OCI shape for the instance (e.g. "VM.Standard.E4.Flex").
                type: string
              shapeConfig:
                description: ShapeConfig specifies the OCPUs and memory for flexible
                  shapes.
                properties:
                  memoryInGBs:
                    description: MemoryInGBs is the total amount of memory available
                      to the instance in GBs.
                    type: number
                  ocpus:
                    description: Ocpus is the number of OCPUs for the instance shape.
                    type: number
                required:
                - memoryInGBs
                - ocpus
                type: object
              subnetId:
                description: SubnetId is the OCID of the subnet in which to create
                  the instance's primary VNIC.
                maxLength: 255
                minLength: 1
                type: string
            required:
            - availabilityDomain
            - compartmentId
            - imageId
            - shape
            - subnetId
            type: object
          status:
            description: ComputeInstanceStatus defines the observed state of ComputeInstance
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
