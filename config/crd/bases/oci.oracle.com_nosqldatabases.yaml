---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: nosqldatabases.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: NoSQLDatabase
    listKind: NoSQLDatabaseList
    plural: nosqldatabases
    singular: nosqldatabase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      priority: 1
      type: string
    - description: status of the NoSQLDatabase
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the NoSQLDatabase
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NoSQLDatabase is the Schema for the nosqldatabases API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NoSQLDatabaseSpec defines the desired state of NoSQLDatabase
            properties:
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the NoSQL table
                maxLength: 255
                minLength: 1
                type: string
              ddlStatement:
                description: DdlStatement is the complete CREATE TABLE DDL statement
                  for this table
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: TableId is the OCID of an existing NoSQL table to bind
                  to (optional; if omitted, a new table is created)
                maxLength: 255
                minLength: 1
                type: string
              name:
                description: Name is the name of the NoSQL table (human-friendly,
                  immutable after creation)
                type: string
              tableLimits:
                description: TableLimits defines throughput and storage limits for
                  the table (required for provisioned capacity)
                properties:
                  maxReadUnits:
                    description: MaxReadUnits is the maximum sustained read throughput
                      limit for the table.
                    minimum: 1
                    type: integer
                  maxStorageInGBs:
                    description: MaxStorageInGBs is the maximum size of storage used
                      by the table, in gigabytes.
                    minimum: 1
                    type: integer
                  maxWriteUnits:
                    description: MaxWriteUnits is the maximum sustained write throughput
                      limit for the table.
                    minimum: 1
                    type: integer
                required:
                - maxReadUnits
                - maxStorageInGBs
                - maxWriteUnits
                type: object
            required:
            - compartmentId
            - ddlStatement
            - name
            type: object
          status:
            description: NoSQLDatabaseStatus defines the observed state of NoSQLDatabase
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
