---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: apigateways.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: ApiGateway
    listKind: ApiGatewayList
    plural: apigateways
    singular: apigateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - jsonPath: .spec.endpointType
      name: EndpointType
      priority: 1
      type: string
    - description: status of the ApiGateway
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ApiGateway
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ApiGateway is the Schema for the apigateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ApiGatewaySpec defines the desired state of ApiGateway
            properties:
              certificateId:
                description: CertificateId is the OCID of a certificate resource to
                  use for HTTPS
                maxLength: 255
                minLength: 1
                type: string
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the gateway
                maxLength: 255
                minLength: 1
                type: string
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              displayName:
                description: DisplayName is a user-friendly name for the gateway
                type: string
              endpointType:
                description: 'EndpointType is the gateway endpoint type: PUBLIC or
                  PRIVATE'
                enum:
                - PUBLIC
                - PRIVATE
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: The OCID of an existing ApiGateway to bind to (optional;
                  if omitted, a new gateway is created)
                maxLength: 255
                minLength: 1
                type: string
              networkSecurityGroupIds:
                description: NetworkSecurityGroupIds is an optional list of NSG OCIDs
                  associated with the gateway
                items:
                  type: string
                type: array
              subnetId:
                description: SubnetId is the OCID of the subnet in which the gateway
                  is created
                maxLength: 255
                minLength: 1
                type: string
            required:
            - compartmentId
            - endpointType
            - subnetId
            type: object
          status:
            description: ApiGatewayStatus defines the observed state of ApiGateway
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
