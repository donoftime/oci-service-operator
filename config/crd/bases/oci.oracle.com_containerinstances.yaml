---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: containerinstances.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: ContainerInstance
    listKind: ContainerInstanceList
    plural: containerinstances
    singular: containerinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ContainerInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ContainerInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ContainerInstance is the Schema for the containerinstances API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ContainerInstanceSpec defines the desired state of ContainerInstance
            properties:
              availabilityDomain:
                description: AvailabilityDomain is the availability domain where the
                  container instance runs.
                type: string
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the container instance.
                maxLength: 255
                minLength: 1
                type: string
              containerRestartPolicy:
                description: ContainerRestartPolicy controls container restart behaviour
                  (ALWAYS, NEVER, ON_FAILURE).
                type: string
              containers:
                description: Containers is the list of containers to run in this instance.
                items:
                  description: ContainerDetails defines a single container in the
                    instance.
                  properties:
                    arguments:
                      description: Arguments are command-line arguments for the container
                        entrypoint.
                      items:
                        type: string
                      type: array
                    command:
                      description: Command overrides the container entrypoint.
                      items:
                        type: string
                      type: array
                    displayName:
                      description: DisplayName is a user-friendly name for the container.
                      type: string
                    environmentVariables:
                      additionalProperties:
                        type: string
                      description: EnvironmentVariables are additional environment
                        variables for the container.
                      type: object
                    imageUrl:
                      description: ImageUrl is the container image URL (e.g. "busybox:latest").
                      type: string
                    resourceConfig:
                      description: ResourceConfig sets per-container resource limits.
                      properties:
                        memoryLimitInGBs:
                          description: MemoryLimitInGBs is the maximum amount of memory
                            (in GB) the container can use.
                          type: number
                        vcpusLimit:
                          description: VcpusLimit is the maximum number of vCPUs the
                            container can use.
                          type: number
                      type: object
                    volumeMounts:
                      description: VolumeMounts defines volume mounts for this container.
                      items:
                        description: ContainerVolumeMount defines a volume mount for
                          a container.
                        properties:
                          isReadOnly:
                            description: IsReadOnly mounts the volume as read-only
                              when true.
                            type: boolean
                          mountPath:
                            description: MountPath is the path inside the container
                              where the volume is mounted.
                            type: string
                          subPath:
                            description: SubPath is an optional path within the volume
                              to mount.
                            type: string
                          volumeName:
                            description: VolumeName is the name of the volume to mount.
                            type: string
                        required:
                        - mountPath
                        - volumeName
                        type: object
                      type: array
                    workingDirectory:
                      description: WorkingDirectory sets the working directory inside
                        the container.
                      type: string
                  required:
                  - imageUrl
                  type: object
                minItems: 1
                type: array
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              displayName:
                description: DisplayName is a user-friendly name for the container
                  instance.
                type: string
              faultDomain:
                description: FaultDomain places the container instance in a specific
                  fault domain.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              gcPolicy:
                description: |-
                  GCPolicy controls garbage collection of old container instances.
                  Defaults to keeping the 3 most recent non-DELETED instances.
                properties:
                  maxInstances:
                    default: 3
                    description: |-
                      MaxInstances is the maximum number of non-DELETED OCI Container Instances
                      to keep for this CR. Must be >= 1. Defaults to 3.
                    format: int32
                    minimum: 1
                    type: integer
                type: object
              gracefulShutdownTimeoutInSeconds:
                description: GracefulShutdownTimeoutInSeconds is the time in seconds
                  for graceful shutdown.
                format: int64
                type: integer
              id:
                description: ContainerInstanceId is the OCID of an existing ContainerInstance
                  to bind to (optional).
                maxLength: 255
                minLength: 1
                type: string
              imagePullSecrets:
                description: ImagePullSecrets provides credentials for pulling images
                  from private registries.
                items:
                  description: ContainerImagePullSecret holds credentials for pulling
                    images from a private registry.
                  properties:
                    password:
                      description: Password is the registry password.
                      type: string
                    registryEndpoint:
                      description: RegistryEndpoint is the registry hostname (e.g.
                        "registry.example.com").
                      type: string
                    username:
                      description: Username is the registry username.
                      type: string
                  required:
                  - password
                  - registryEndpoint
                  - username
                  type: object
                type: array
              shape:
                description: Shape is the OCI shape for the container instance (e.g.
                  "CI.Standard.E4.Flex").
                type: string
              shapeConfig:
                description: ShapeConfig specifies the OCPUs and memory for the shape.
                properties:
                  memoryInGBs:
                    description: MemoryInGBs is the total amount of memory in GBs.
                    type: number
                  ocpus:
                    description: Ocpus is the number of OCPUs for the shape.
                    type: number
                required:
                - memoryInGBs
                - ocpus
                type: object
              vnics:
                description: Vnics defines the networking configuration for the container
                  instance.
                items:
                  description: ContainerVnicDetails defines the networking configuration
                    for a container instance VNIC.
                  properties:
                    displayName:
                      description: DisplayName is a user-friendly name for the VNIC.
                      type: string
                    nsgIds:
                      description: NsgIds is a list of NSG OCIDs to associate with
                        this VNIC.
                      items:
                        maxLength: 255
                        minLength: 1
                        type: string
                      type: array
                    subnetId:
                      description: SubnetId is the OCID of the subnet for the VNIC.
                      maxLength: 255
                      minLength: 1
                      type: string
                  required:
                  - subnetId
                  type: object
                minItems: 1
                type: array
            required:
            - availabilityDomain
            - compartmentId
            - containers
            - shape
            - shapeConfig
            - vnics
            type: object
          status:
            description: ContainerInstanceStatus defines the observed state of ContainerInstance
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
