---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: containerinstances.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: ContainerInstance
    listKind: ContainerInstanceList
    plural: containerinstances
    singular: containerinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the ContainerInstance
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the ContainerInstance
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: ContainerInstance is the Schema for the containerinstances API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ContainerInstanceSpec defines the desired state of ContainerInstance
            properties:
              availabilityDomain:
                description: AvailabilityDomain is the availability domain where the container instance runs.
                type: string
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which to create the container instance.
                maxLength: 255
                minLength: 1
                type: string
              containerRestartPolicy:
                description: ContainerRestartPolicy controls container restart behaviour (ALWAYS, NEVER, ON_FAILURE).
                type: string
              containers:
                description: Containers is the list of containers to run in this instance.
                items:
                  description: ContainerDetails defines a single container in the instance.
                  properties:
                    arguments:
                      description: Arguments are command-line arguments for the container entrypoint.
                      items:
                        type: string
                      type: array
                    command:
                      description: Command overrides the container entrypoint.
                      items:
                        type: string
                      type: array
                    displayName:
                      description: DisplayName is a user-friendly name for the container.
                      type: string
                    environmentVariables:
                      additionalProperties:
                        type: string
                      description: EnvironmentVariables are additional environment variables for the container.
                      type: object
                    imageUrl:
                      description: 'ImageUrl is the container image URL (e.g. "busybox:latest").'
                      type: string
                    resourceConfig:
                      description: ResourceConfig sets per-container resource limits.
                      properties:
                        memoryLimitInGBs:
                          description: MemoryLimitInGBs is the maximum amount of memory (in GB) the container can use.
                          type: number
                        vcpusLimit:
                          description: VcpusLimit is the maximum number of vCPUs the container can use.
                          type: number
                      type: object
                    workingDirectory:
                      description: WorkingDirectory sets the working directory inside the container.
                      type: string
                  required:
                  - imageUrl
                  type: object
                minItems: 1
                type: array
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              displayName:
                description: DisplayName is a user-friendly name for the container instance.
                type: string
              faultDomain:
                description: FaultDomain places the container instance in a specific fault domain.
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              gracefulShutdownTimeoutInSeconds:
                description: GracefulShutdownTimeoutInSeconds is the time in seconds for graceful shutdown.
                format: int64
                type: integer
              id:
                description: ContainerInstanceId is the OCID of an existing ContainerInstance to bind to (optional).
                maxLength: 255
                minLength: 1
                type: string
              shape:
                description: 'Shape is the OCI shape for the container instance (e.g. "CI.Standard.E4.Flex").'
                type: string
              shapeConfig:
                description: ShapeConfig specifies the OCPUs and memory for the shape.
                properties:
                  memoryInGBs:
                    description: MemoryInGBs is the total amount of memory in GBs.
                    type: number
                  ocpus:
                    description: Ocpus is the number of OCPUs for the shape.
                    type: number
                required:
                - memoryInGBs
                - ocpus
                type: object
              vnics:
                description: Vnics defines the networking configuration for the container instance.
                items:
                  description: ContainerVnicDetails defines the networking configuration for a container instance VNIC.
                  properties:
                    displayName:
                      description: DisplayName is a user-friendly name for the VNIC.
                      type: string
                    nsgIds:
                      description: NsgIds is a list of NSG OCIDs to associate with this VNIC.
                      items:
                        description: "+kubebuilder:validation:MaxLength=255\n+kubebuilder:validation:MinLength=1"
                        maxLength: 255
                        minLength: 1
                        type: string
                      type: array
                    subnetId:
                      description: SubnetId is the OCID of the subnet for the VNIC.
                      maxLength: 255
                      minLength: 1
                      type: string
                  required:
                  - subnetId
                  type: object
                minItems: 1
                type: array
            required:
            - availabilityDomain
            - compartmentId
            - containers
            - shape
            - shapeConfig
            - vnics
            type: object
          status:
            description: ContainerInstanceStatus defines the observed state of ContainerInstance
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            required:
            - status
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
