---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: postgresdbsystems.oci.oracle.com
spec:
  group: oci.oracle.com
  names:
    kind: PostgresDbSystem
    listKind: PostgresDbSystemList
    plural: postgresdbsystems
    singular: postgresdbsystem
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      priority: 1
      type: string
    - description: status of the PostgresDbSystem
      jsonPath: .status.status.conditions[-1].type
      name: Status
      type: string
    - description: Ocid of the PostgresDbSystem
      jsonPath: .status.status.ocid
      name: Ocid
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: PostgresDbSystem is the Schema for the postgresdbsystems API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PostgresDbSystemSpec defines the desired state of PostgresDbSystem
            properties:
              adminPassword:
                description: AdminPassword is the admin password for the PostgreSQL
                  DB system, read from a Kubernetes secret
                properties:
                  secret:
                    properties:
                      secretName:
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: adminPassword is immutable
                  rule: self == oldSelf
              adminUsername:
                description: AdminUsername is the admin username for the PostgreSQL
                  DB system, read from a Kubernetes secret
                properties:
                  secret:
                    properties:
                      secretName:
                        type: string
                    type: object
                type: object
                x-kubernetes-validations:
                - message: adminUsername is immutable
                  rule: self == oldSelf
              compartmentId:
                description: CompartmentId is the OCID of the compartment in which
                  to create the PostgreSQL DB system
                maxLength: 255
                minLength: 1
                type: string
              dbVersion:
                description: DbVersion is the PostgreSQL version (e.g. "14.10")
                type: string
                x-kubernetes-validations:
                - message: dbVersion is immutable
                  rule: self == oldSelf
              definedTags:
                additionalProperties:
                  additionalProperties:
                    type: string
                  type: object
                type: object
              description:
                description: Description is an optional user-provided description
                  of the DB system
                type: string
              displayName:
                description: DisplayName is a user-friendly name for the PostgreSQL
                  DB system
                type: string
              freeformTags:
                additionalProperties:
                  type: string
                type: object
              id:
                description: The OCID of an existing PostgresDbSystem to bind to (optional;
                  if omitted, a new DB system is created)
                maxLength: 255
                minLength: 1
                type: string
              instanceCount:
                description: InstanceCount is the number of database instance nodes
                  (defaults to 1)
                minimum: 1
                type: integer
                x-kubernetes-validations:
                - message: instanceCount is immutable
                  rule: self == oldSelf
              instanceMemoryInGBs:
                description: InstanceMemoryInGBs is the total memory available to
                  each instance node, in gigabytes
                type: integer
                x-kubernetes-validations:
                - message: instanceMemoryInGBs is immutable
                  rule: self == oldSelf
              instanceOcpuCount:
                description: InstanceOcpuCount is the total OCPUs available to each
                  instance node
                type: integer
                x-kubernetes-validations:
                - message: instanceOcpuCount is immutable
                  rule: self == oldSelf
              shape:
                description: Shape is the instance shape for the DB system nodes (e.g.
                  "VM.Standard.E4.Flex")
                type: string
                x-kubernetes-validations:
                - message: shape is immutable
                  rule: self == oldSelf
              storageType:
                description: StorageType is an optional hint for storage selection;
                  currently the OCI Optimized storage tier is always used
                type: string
                x-kubernetes-validations:
                - message: storageType is immutable
                  rule: self == oldSelf
              subnetId:
                description: SubnetId is the OCID of the subnet for the DB system
                maxLength: 255
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: subnetId is immutable
                  rule: self == oldSelf
            required:
            - compartmentId
            - dbVersion
            - displayName
            - shape
            - subnetId
            type: object
          status:
            description: PostgresDbSystemStatus defines the observed state of PostgresDbSystem
            properties:
              status:
                properties:
                  conditions:
                    items:
                      properties:
                        lastTransitionTime:
                          format: date-time
                          type: string
                        message:
                          type: string
                        reason:
                          type: string
                        status:
                          type: string
                        type:
                          type: string
                      required:
                      - status
                      - type
                      type: object
                    type: array
                  createdAt:
                    format: date-time
                    type: string
                  deletedAt:
                    format: date-time
                    type: string
                  message:
                    type: string
                  ocid:
                    maxLength: 255
                    minLength: 1
                    type: string
                  reason:
                    type: string
                  requestedAt:
                    format: date-time
                    type: string
                  updatedAt:
                    format: date-time
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
